<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Lista de productos</title>
</head>
<body>
  <h1>Lista de productos</h1>
  <ul>
    {{#each products}}
    <li>
      <h2>{{title}}</h2>
      <p>{{description}}</p>
      <p>Precio: {{price}}</p>
      <p>Código: {{code}}</p>
      <p>Categoria: {{category}}</p>
    </li>
    {{/each}}
  </ul>
</body>
</html>